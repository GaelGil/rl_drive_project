[package]
name = "asteroids_python"
version = "0.1.0"
edition = "2024"

[dependencies]
asteroids_core = { version = "0.1.0", path = "../asteroids_core" }
pyo3 = { version = "0.29.0", features = ["extension-module"] }
